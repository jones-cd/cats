Uses JavaScript.
It connects to the API and creates a card for each of the breeds, detailing a description and country of origin on each.

I would seek to include an image on each card and a 'select' dropdown menu so that only one card is displayed at a time.